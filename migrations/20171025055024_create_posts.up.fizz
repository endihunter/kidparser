create_table("posts", func(t) {
	t.Column("id", "uuid", {"primary": true})
	t.Column("user_id", "integer", {})
	t.Column("title", "string", {})
	t.Column("body", "text", {})
})

raw("alter table `posts` add constraint `posts_user_id_foreign` foreign key (`user_id`) references `users`(`id`)")